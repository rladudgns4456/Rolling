import React from 'react';

const Header = () => {
  return (
    <header className="flex justify-between bg-blue-900 text-cyan-50">
      <div>Rolling</div>
      <div>
        <button className="">롤링 페이퍼 만들기</button>
      </div>
    </header>
  );
};

export default Header;
